# Instance Folder

This folder is used for runtime files such as the SQLite database.

- The database file is created automatically on first run.
- Database files are ignored by Git for security reasons.
